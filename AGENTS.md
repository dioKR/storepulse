# AGENTS.md — storepulse 협업 규칙

이 문서는 이 저장소에서 작업하는 모든 자동화 에이전트(구현·리뷰)와 사람이 따르는 규칙이다.

## 작업 방식

- 모든 변경은 PR로 진행한다. **main 직접 push 금지, 직접 merge 금지** — 최종 merge는 메인테이너가 한다.
- 자동화 에이전트의 브랜치는 `claude/<작업-요약>` 형식을 쓴다.
- **한 PR = 한 목적.** 리팩터링·문서·기능을 불필요하게 섞지 않는다.
- 커밋 메시지와 PR 제목에 AI 도구 이름을 언급하지 않는다. Co-Authored-By 트레일러도 넣지 않는다.
- 작업 시작 전 `git status`와 최근 로그를 확인하고, 진행 중인 다른 작업과 파일·기능 범위가 겹치면 수정 전에 먼저 보고한다.

## 검증과 PR 형식

- PR 전 필수 게이트: `pnpm lint` / `pnpm typecheck` / `pnpm test` — 결과를 PR 본문에 적는다.
- PR 본문(한국어): ① 목적 ② 변경 범위 ③ 테스트 결과 ④ 호환성·문서·changeset 영향 ⑤ 의도적으로 제외한 범위.
- 다음을 변경할 때는 하위 호환성·테스트·문서·changeset 필요 여부를 명시적으로 점검한다:
  공개 API(@storepulse/core), CLI 명령·옵션, `status.json` 스키마, 외부 스토어 API 처리.
- `status.json` 스키마 규칙: 정수 `schemaVersion`, **선택 필드 추가는 버전을 올리지 않는다** (`docs/snapshot-schema.md`).
- 기능 머지 후 문서 5개 언어(README 5종 + `site/` 10페이지) 동기화는 별도 PR로 한다.

## 자동 리뷰 대응 (리뷰 루프 종결 규칙)

- **심각도별 대응**: P0·P1 → 해당 PR에서 수정. P2 → 타당하고 작으면 수정, 크면 이슈로 이관하고
  사유를 스레드 답글로 남긴 뒤 resolve. P3·스타일 → 원칙적으로 이슈화만.
- **라운드 상한**: 재검토 요청(`@codex review`)은 **PR당 최대 2회**. 상한 소진 후의 지적은
  수정하더라도 재검토를 다시 부르지 않고 "답글 + resolve"로 닫은 뒤 사람의 최종 판단에 맡긴다.
  P0(치명 결함)만 상한과 무관하게 수정한다.
- **재검토 호출 조건**: 실질적인 코드 변경이 있을 때만. 주석·문서·테스트 이름 변경으로는 부르지 않는다.
- **이관 기록**: 이슈로 넘긴 지적은 이슈 번호를 리뷰 스레드 답글에 남긴다.
- 리뷰가 실행되지 않거나 실패하면 그 사실을 명확히 보고하고, 통과했다고 가정하지 않는다.

## 보안

- 크리덴셜·토큰·개인정보를 코드, 로그, 에러 메시지, PR·이슈 본문에 절대 포함하지 않는다.
- 시크릿은 `.env`(로컬)와 CI Secrets에만 존재한다. `storepulse.config.json`(실제 앱 목록)은 커밋하지 않는다.
- GitHub Actions: 서드파티 액션은 커밋 SHA로 핀(버전 주석 병기), `permissions` 최소화,
  시크릿을 쓰는 워크플로우에 `pull_request` 트리거 금지.
- 문서에는 스토어 콘솔 스크린샷을 싣지 않는다(개인정보·UI 변경 취약) — 텍스트 경로 + 공식 문서 링크로 안내한다.

## i18n

- 사용자 대상 문자열(CLI·대시보드)은 `packages/core/src/i18n`(en/ko) 사전을 경유한다.
  배지·컬럼 헤더 같은 용어는 영어로 고정한다.
- 대시보드 i18n 번들은 `node scripts/gen-dashboard-i18n.mjs`로 재생성한다.
  포맷터가 생성 파일을 재포맷할 수 있으므로 **`pnpm lint:fix` → 번들 재생성 → `pnpm lint` 확인** 순서를 지킨다
  (동기화는 테스트로 강제된다).
