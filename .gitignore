node_modules/
dist/
.astro/
*.tsbuildinfo

# local-only: your real config & credentials (the *.example.* files are the committed ones)
.env
storepulse.config.json
*.p8
service-account*.json
.DS_Store
.claude/
