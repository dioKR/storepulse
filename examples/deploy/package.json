{
  "name": "storepulse-cloudflare-deploy",
  "version": "0.0.0",
  "private": true,
  "description": "Copy-ready Cloudflare Pages deployment for a private storepulse board",
  "engines": {
    "node": ">=20.12"
  },
  "scripts": {
    "build:demo": "mkdir -p site && cp -R node_modules/storepulse/dist/dashboard/. site/ && storepulse snapshot --demo --out site/status.json"
  },
  "devDependencies": {
    "storepulse": "0.9.0",
    "wrangler": "4.120.0"
  }
}
