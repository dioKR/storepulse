{
  "name": "storepulse-monorepo",
  "private": true,
  "version": "0.0.0",
  "description": "Monorepo for storepulse — see packages/core and packages/cli",
  "scripts": {
    "demo": "tsx packages/cli/src/index.ts demo",
    "status": "tsx packages/cli/src/index.ts",
    "typecheck": "tsc --build",
    "build": "tsc --build",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "test": "vitest run",
    "changeset": "changeset",
    "release": "pnpm build && changeset publish"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.5",
    "@changesets/cli": "^2.31.1",
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^4.1.10"
  },
  "packageManager": "pnpm@9.12.2"
}
