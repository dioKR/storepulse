---
"@storepulse/core": minor
"storepulse": minor
---

Add `storepulse check --fail-on <state,...>` — a CI policy gate with a fixed exit-code contract (0 = no violations, 1 = policy violations, 2 = execution error, including any target that could not be fetched), `--format text|json` output and `--demo` support.
